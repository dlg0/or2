{
  "name": "openworld-kids-game",
  "private": true,
  "version": "0.1.0",
  "packageManager": "npm@11.3.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev --parallel",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "format": "prettier --write ."
  },
  "overrides": {
    "@colyseus/core": {
      "nanoid": "^2.1.11"
    },
    "postcss": {
      "nanoid": "^3.3.11"
    }
  },
  "devDependencies": {
    "prettier": "latest",
    "turbo": "latest",
    "typescript": "latest"
  }
}
