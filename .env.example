# Shared
WORLD_SEED=demo-world-001

# Clerk (parent accounts only at MVP)
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=
CLERK_SECRET_KEY=

# Server
PORT=2567

# Web Client
NEXT_PUBLIC_SERVER_URL=ws://localhost:2567
# Tick rate for server patch broadcasting (Hz). Higher = more frequent updates.
# Reasonable values: 30, 60 (default 60 if unset)
TICK_HZ=60
REQUIRE_KID_AUTH=1
# Token secret for kid auth (HMAC). Set to a long random string in prod.
TOKEN_SECRET=change-me-dev-secret

# Postgres (Neon) connection string — project: "octorobot"
# Example: postgres://user:password@ep-xxxx.neon.tech/octorobot?sslmode=require
DATABASE_URL=postgres://USER:PASSWORD@HOST/DBNAME?sslmode=require

# Clerk (Auth) — Web (Vercel)
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=pk_test_xxx
CLERK_SECRET_KEY=sk_test_xxx
# Optional: if using JWT templates on the game server
CLERK_JWT_KEY=b64_jwks_or_public_key

# Database
DATABASE_URL=postgres://user:pass@host:5432/db

# Redis
REDIS_URL=redis://default:pass@host:6379
